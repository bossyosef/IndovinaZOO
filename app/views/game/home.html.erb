<h1>IndovinaZOO 1.0</h1>

<script>
  var idbSupported = false;
  document.addEventListener("DOMContentLoaded", function()
  {
    if("indexedDB" in window) 
    {
        idbSupported = true;
        //mostra la form
    } else 
    {
        console.log("Fucking HTML5...");
        //mostra frase che dice di cambiare browser
    }
  },false);

</script>

<div id="mostra_gioco" >
  <%= button_to "Prima imposta le preferenze...", settings_path %>
  <%= form_tag("/quiz/1", method: "post") do %>
      <%= hidden_field_tag "numeroquiz", "10" %>
      <%= hidden_field_tag "timeout", "60" %>
      <%= submit_tag("Gioca adesso!") %>
  <% end %>
</div>

<div id="cambio_browser" >
  Ci dispiace, ma il browser corrente non è adatto all'esecuzione di IndovinaZOO, noi ti consigliamo Chrom{ium,e}.
</div>

<%= link_to "Guarda gli animali!", animals_path %>
